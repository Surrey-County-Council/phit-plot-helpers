# Setup vscode
- users will need the extentions for vscode

# Setup python
- user should have uv installed (for users with an existing python run `pip install uv`)

# Setup R
- user should have r installed.
- r can be invoked from `uv run radian` and will use the configured environment.
- users can add dependencies to the renv from the interactive terminal.
